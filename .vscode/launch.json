{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Current File (64-bit)",
            "type": "python",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "pythonPath": "${workspaceFolder}/.venv64/Scripts/python.exe"
        }
    ]
}
