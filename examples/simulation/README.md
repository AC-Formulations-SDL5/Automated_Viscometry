# Simulation (no hardware)

Hardware-free demo of the web UI.

## Prerequisites

From the repository root:

```bash
python3 -m pip install -e .
```

## Run

```bash
python3 examples/simulation/run_simulation.py
```

Or with auto browser open:

```bash
python3 examples/simulation/start_with_browser.py
```
